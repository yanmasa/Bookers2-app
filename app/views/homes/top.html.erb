<div class="container">
  <div class="row">
    <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6">
      <h1>welcome to <i class="fas fa-book"></i><strong>Bookers</strong>  !!</h1>
      <p>In  <i class="fas fa-book"></i><strong>Bookers</strong>, </p>
      <p>you can share and exchange  your opinions , inpressions , and emotions</p>
      <p> about various books!</p>
      <div class="btn-wrapper col-10 mx-auto">
        <div class="row">
          <% if user_signed_in? %>
            <%= link_to "Log in", user_path(current_user.id), class: "btn btn-info btn-lg btn-block mb-3" %>
          <% else %>
            <%= link_to "Log in", new_user_session_path, class: "btn btn-info btn-lg btn-block mb-3"  %>
          <% end %>
        </div>
        <div class="row">
          <% if user_signed_in? %>
            <%= link_to "Sign Up", user_path(current_user.id), class: "btn btn-success btn-lg btn-block" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success btn-lg btn-block" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>